/**
 * *13. Find the absolute value of a number without using Math.abs().
 */

let num = 40;

if (num < 0) {
  console.log(`Absolute value of ${num} is ${-num}`);
} else {
  console.log(`Absolute value of ${num} is ${num}`);
}
