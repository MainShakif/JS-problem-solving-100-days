/**
 * *13. Find the absolute value of a number without using Math.abs().
 */

let num = -80;

if (num < 0) {
  console.log("Absolute value is:", -num);
} else {
  console.log(
    "This is a positive number or zero, Do not need to convert it to its positive value",
  );
}
